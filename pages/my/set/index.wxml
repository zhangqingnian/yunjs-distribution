<!--pages/my/set/index.wxml-->
<view class='containner'>
    <view class='list'>
        <view wx:for="{{arr}}" wx:key="{{item.type}}" 
            bindtap='onSelect' data-item='{{item}}'>{{item.name}}</view>
    </view>

    <view class='submit' bindtap='onLogout'>退出登录</view>
</view>
