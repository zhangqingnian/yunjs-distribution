<!--pages/webviwe/index.wxml-->
<view class='warp'>
    <image src='{{imgUrl + item.activityImg}}' mode='widthFix'></image>
    <view class='bottomwarp flex_row' wx:if="{{item.activity == 3}}">
        <view class='confirm' bindtap='onGoDetail'>
            进入任务
        </view>
    </view>
</view>
